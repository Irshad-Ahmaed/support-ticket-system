<h2>Your Tickets</h2>
<ul>
  <% tickets.forEach(ticket => { %>
    <li>
      <%= ticket.title %> - <%= ticket.status %> -
      <a href="<%= ticket.github_issue_url %>" target="_blank">View on GitHub</a>
    </li>
  <% }) %>
</ul>